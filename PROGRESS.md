# PROGRESS.md — Aplikasi Form Seleksi Investasi (multi-form)

Status legend: `[ ]` belum · `[~]` sedang dikerjakan · `[x]` selesai · `[-]` dilewati (keputusan sadar, bukan belum sempat)

Setiap kali sebuah tahap selesai, checkbox diupdate dan dibuat 1 git commit yang menyebutkan tahap tersebut secara eksplisit (contoh: `feat: selesaikan tahap 1.1 - form Bagian A`).

---

## FASE SETUP

- [x] 0.1 Setup project Next.js, Supabase, struktur folder multi-form
- [x] 0.2 Skema database (forms, submissions, approval_chain, submission_history, user_roles) — **dijalankan & diverifikasi** di project Supabase asli
- [x] 0.3 Autentikasi Supabase (login, role: Evaluator/Manajer/VP/Direksi/Admin) — **diuji end-to-end**: login/logout 5 role (evaluator/manajer/vp/direksi/admin) semua berhasil
- [x] 0.4 Landing page dasar (daftar form, saat ini baru 1: Seleksi Investasi) — **diverifikasi di browser**: daftar form tampil, ringkasan per role benar ("Ada N proposal menunggu keputusan Anda", dst)

## FORM SELEKSI INVESTASI — BAGIAN A (Identitas)

- [x] 1.1 Form input Bagian A: identitas proposal (10 field) — **diuji end-to-end** 2x (submission lengkap & submission gagal gate): auto-suggest nomor registrasi (increment benar), default tanggal & target selesai (H+5 hari kerja) benar, CurrencyField format ribuan benar

## FORM SELEKSI INVESTASI — BAGIAN B (Gate)

- [x] 2.1 Form input Bagian B: 6 kriteria gugur (Ya/Tidak + bukti/catatan) — **diuji end-to-end**: badge real-time "Belum lengkap"→"Lulus Gate"/"Tidak Lulus" berubah persis saat radio diklik
- [x] 2.2 Logika auto-stop: jika ada 1 "Tidak", submission otomatis TIDAK DILANJUTKAN, skip Bagian C — **diuji end-to-end** kedua jalur: semua "Ya" → lanjut Bagian C; satu "Tidak" (G3) → "TIDAK DILANJUTKAN" dengan rincian kriteria gagal, Bagian C dikonfirmasi terblokir (fetch langsung ke route-nya)

## FORM SELEKSI INVESTASI — BAGIAN C (Skoring)

- [x] 3.1 Form input Bagian C: 8 kriteria skoring (skor 1-5 + justifikasi), muncul HANYA jika lulus gate — **diuji end-to-end**
- [x] 3.2 Tampilkan rubrik skoring (skor 1/3/5) sebagai referensi/tooltip saat isi tiap kriteria — **diuji end-to-end**: klik tombol "?" C1, teks rubrik yang tampil persis sama dengan isi `Rubik.xlsx`
- [x] 3.3 Kalkulasi otomatis: nilai tertimbang per kriteria (bobot x skor) dan total skor — **diuji end-to-end**: 8 nilai tertimbang + total (3.55) di browser cocok 100% dengan hitungan manual

## FORM SELEKSI INVESTASI — BAGIAN D (Hasil & Approval Bertingkat)

- [x] 4.1 Logika rekomendasi otomatis berdasarkan total skor (4 ambang batas) — **diuji end-to-end**: skor 3.55 → "PRIORITAS B" tampil benar
- [x] 4.2 Field catatan evaluator, pernyataan bebas benturan kepentingan — **diuji end-to-end**
- [x] 4.3 Alur approval bertingkat: Manajer -> VP -> Direksi, dengan opsi hentikan/teruskan jika ditolak — **diuji end-to-end penuh**: Manajer tolak → Evaluator pilih "teruskan" (label audit "diteruskan atas permintaan Evaluator" tampil) → VP setuju → Direksi setuju (final) → status akhir "Disetujui". Jalur "Hentikan proses" **belum dicoba langsung** (hanya code review) — cukup yakin karena polanya simetris dengan "teruskan", tapi tandai sebagai gap kecil kalau mau extra teliti.
- [x] 4.4 Dashboard/riwayat submission (list semua proposal, beda tampilan per role) — **diuji end-to-end**: tab Manajer/VP "Menunggu Keputusan Saya", indikator "perlu keputusan Anda" untuk evaluator saat ada penolakan pending

## EXPORT & FINALISASI

- [x] 5.1 Export PDF hasil evaluasi (layout sesuai form Excel, area tanda tangan 4 pihak) — **diuji end-to-end** dengan submission asli yang sudah disetujui penuh: fetch ke route PDF, status 200, `%PDF-1.3` valid, 7784 bytes
- [x] 5.2 Testing end-to-end alur lengkap — **selesai**, lihat ringkasan di bawah
- [x] 5.3 Deploy ke Vercel — **selesai**. Repo di [github.com/ftriantoro-byte/form-seleksi-investasi](https://github.com/ftriantoro-byte/form-seleksi-investasi), live di **[form-seleksi-investasi.vercel.app](https://form-seleksi-investasi.vercel.app)**, terverifikasi (halaman login termuat benar, bukan error). Satu masalah ditemukan & diperbaiki saat deploy: lihat catatan "Sensitive env var" di bawah.

**Semua tahap FASE SETUP s/d EXPORT & FINALISASI selesai dan terverifikasi. Form Seleksi Investasi production-ready.**

---

## FORM QUICK SCREEN PROYEK — SETUP

- [x] 0.1 Migration: generalisasi trigger `buat_approval_chain_awal()` (kolom `forms.tingkat_approval_maksimal`, default `direksi` sehingga Seleksi Investasi tidak berubah) + seed row `forms` untuk `quick-screen-proyek` (`tingkat_approval_maksimal = 'vp'`)

## FORM QUICK SCREEN PROYEK — BAGIAN A (Identitas Proyek)

- [x] 1.1 Form input Bagian A (11 field: No. Dokumen, Tanggal, Status Dokumen, Nama Proyek, Sektor/Tipologi, Pemilik/Pemberi Informasi, Lokasi, Tanggal Info Diterima, Estimasi Nilai, Sumber Informasi, Deskripsi) — **diuji end-to-end** di browser: auto-suggest No. Dokumen "QSP-2026-001" lalu "QSP-2026-002" (increment benar per tahun), CurrencyField format ribuan benar

## FORM QUICK SCREEN PROYEK — BAGIAN B (Skema Kerjasama)

- [x] 2.1 Tabel 8 opsi skema kerjasama (Ya/Tidak + catatan opsional), bukan gate pass/fail — **diuji end-to-end**: pilih 2 dari 8 skema (Design & Build, BOT), submit, tampil benar dengan tanda ✓/– di halaman review

## FORM QUICK SCREEN PROYEK — BAGIAN C (Penilaian 4 Dimensi)

- [x] 3.1 Form input 20 kriteria skala H/M/L (skor 3/2/1), 4 dimensi x 5 kriteria, catatan opsional per kriteria — **diuji end-to-end** 2x (skor campuran & skor minimum semua)
- [x] 3.2 Kalkulasi skor per dimensi & total skor (maks 60) + radar chart live (Recharts, 4 sumbu skala 0-15) — **diuji end-to-end**: skor campuran (Pasar 13, Teknis 13, Legal 10, Skema&Finansial 7) → total 43, cocok 100% dengan hitungan manual; radar chart live update saat radio diklik
- [x] 3.3 Logika hasil otomatis GO/GO BERSYARAT/CAUTION/NO-GO berdasarkan ambang skor (48/36/28) — **diuji end-to-end** 2 titik: skor 43 → "GO BERSYARAT" (rentang 36-47) benar; skor 20 (semua L) → "NO-GO" (<28) benar. Titik batas 48/36/28 lainnya belum dicoba langsung di browser (hanya code review terhadap `getHasilQuickScreen()`), tapi logikanya if/else sederhana beruntun — risiko rendah

## FORM QUICK SCREEN PROYEK — BAGIAN D & APPROVAL

- [x] 4.1 Field catatan analis (Faktor Pendorong, Faktor Risiko, Data/Dokumen untuk OFT, Urgensi & Tenggat) — **diuji end-to-end**
- [x] 4.2 Alur approval 2 tingkat (Manajer → VP), Setuju/Tolak biasa, tolak di tingkat manapun = langsung final ditolak (tanpa hentikan/teruskan) — **diuji end-to-end penuh** 2 jalur: (a) Manajer setuju → VP setuju (final, badge "tanpa eskalasi" tampil) → status "Disetujui"; (b) submission kedua, Manajer tolak → status langsung "Ditolak" final, tidak ada prompt hentikan/teruskan ke evaluator. Trigger `buat_approval_chain_awal()` terkonfirmasi hanya buat 2 baris approval_chain (manajer, vp) untuk form ini
- [x] 4.3 Dashboard (`/dashboard`) digeneralisasi lintas form (join `forms`, kolom Form, fallback field Bagian A, link dinamis per slug) — **diuji end-to-end**: submission Quick Screen & Seleksi Investasi tampil bersamaan di satu tabel, kolom "Form" membedakan keduanya, data lama Seleksi Investasi (No. Registrasi, skor format 2 desimal, link) tidak berubah

## EXPORT & FINALISASI QUICK SCREEN PROYEK

- [x] 5.1 Export PDF (identitas proyek, skema kerjasama terpilih, skor 4 dimensi + radar chart digambar manual, badge hasil, catatan analis, area tanda tangan Manajer & VP) — **diuji end-to-end**: fetch ke route PDF, status 200, `%PDF-1.3` valid, 5992 bytes, `Content-Type: application/pdf`
- [x] 5.2 Testing end-to-end alur lengkap (A→D, approval Manajer→VP, reject langsung final, PDF) — **selesai**, lihat detail di setiap butir di atas plus regresi Seleksi Investasi di bawah
- [ ] 5.3 Commit & push ke repo yang sama (Vercel auto-deploy) — commit lokal sudah dibuat per tahap, **push ke GitHub belum dilakukan** (menunggu konfirmasi user)

**Regresi nol terhadap Seleksi Investasi dikonfirmasi:** submission baru form Seleksi Investasi (PRP-2026-035) dibuat penuh A→D setelah migration generalisasi trigger dijalankan — approval_chain tetap otomatis berisi 3 baris (Manajer/VP/Direksi, semua "Menunggu") persis seperti sebelumnya. `StepIndicator` form Seleksi Investasi juga tidak berubah tampilannya (masih A/B/C/D dengan label Identitas/Gate/Skoring/Hasil, karena parameter `steps` di komponen itu opsional dengan default sama).

---

## Ringkasan Testing End-to-End (tahap 5.2) — 2026-07-09/10

Dijalankan langsung di browser (Claude Preview) terhadap project Supabase asli milik user, bukan simulasi. Skenario yang lolos:

1. Evaluator (`budi@gmail.com`) buat submission, isi Bagian A → semua default & auto-suggest benar.
2. Bagian B semua "Ya" → lulus gate → lanjut Bagian C.
3. Bagian C 8 skor campuran → kalkulasi & rekomendasi (PRIORITAS B, skor 3.55) benar.
4. Bagian D → submit → status `menunggu_manajer`, 3 baris `approval_chain` otomatis dibuat (trigger).
5. Manajer tolak (dengan catatan) → status TETAP `menunggu_manajer` (menunggu keputusan evaluator, sesuai desain).
6. Evaluator pilih "Tetap teruskan" → status maju ke `menunggu_vp`, label transparansi audit tampil.
7. VP setuju → status `menunggu_direksi`.
8. Direksi setuju (final, tanpa opsi teruskan) → status `disetujui`.
9. Download PDF submission yang sudah lengkap → valid.
10. Submission kedua: Bagian B dengan 1 kriteria "Tidak" (G3) → "TIDAK DILANJUTKAN", Bagian C dikonfirmasi terblokir.
11. Role guard: Direksi mencoba akses `/forms/seleksi-investasi/baru` → ditolak dengan pesan yang benar.

**3 bug nyata ditemukan & diperbaiki selama testing ini** (lihat riwayat commit untuk detail lengkap):
- Nama policy RLS mengandung spasi rawan rusak saat copy-paste ke SQL Editor (`4565d6b`).
- Bug timezone di `tambahHariKerja()` dan default tanggal — hasil mundur 1 hari di server berzona WIB (`c0054ef`).
- **Infinite recursion RLS** antara `submissions` dan `approval_chain` (dua percobaan fix: `a018258` lalu `7f596a3` yang benar-benar menyelesaikannya — pelajaran: fungsi helper RLS harus `plpgsql`, bukan `sql`, supaya tidak di-inline planner).

Belum diuji eksplisit (gap kecil, bukan blocker): jalur "Hentikan proses" (evaluator menghentikan setelah penolakan), tampilan dashboard sebagai admin dengan banyak data, submission_history secara langsung (hanya diverifikasi tidak sengaja lewat log server, bukan lewat UI - belum ada halaman yang menampilkannya).

## Catatan Terbuka / Keputusan Tertunda

- Detail lengkap tiap tahap (nama field Bagian A, teks 6 kriteria gate Bagian B, 8 kriteria skoring+bobot Bagian C, 4 ambang batas rekomendasi Bagian D, alur approval bertingkat) ada di [PANDUAN.md](PANDUAN.md) — sumber kebenaran, jangan diparafrase ulang saat implementasi.
- "Target selesai seleksi awal" default H+5 hari kerja dihitung sekali di server saat halaman dimuat (dari tanggal hari ini), **tidak recompute otomatis** kalau user mengubah tanggal diterima di form (butuh Client Component + JS kalau mau live-update — belum dibangun, dianggap di luar cakupan minimal tahap 1.1). User tetap bisa edit manual field targetSelesai.
- Setelah Bagian C disubmit, `status` submission **tetap** `menunggu_skoring` (tidak berubah ke `menunggu_manajer`) — transisi ke approval bertingkat baru terjadi setelah Bagian D lengkap (termasuk pernyataan bebas benturan kepentingan), sesuai tahap 4.3.
- Beberapa keputusan desain untuk resolusi ambiguitas PANDUAN.md di tahap 4.1-4.3 (rekomendasi bukan status tersendiri, approval tetap jalan penuh berapa pun skornya, "PARKIR - evaluasi ulang maks 1x" belum ada mekanismenya) dicatat lengkap di ARCHITECTURE.md §6.
- "PARKIR - minta perbaikan proposal, evaluasi ulang maksimal 1 kali": teks rekomendasi ini persis dari PANDUAN.md, tapi **mekanisme evaluasi ulang/revisi proposal belum ada** (tidak ada counter, tidak ada alur resubmit). Jika dibutuhkan, ini scope tambahan di luar checklist asli — tanyakan ke user dulu sebelum membangunnya.
- Sanity-check manual (di luar aplikasi, tanpa DB): ambang batas rekomendasi 4.1 dicoba di titik batas (4.00, 3.99, 3.00, 2.99, 2.00, 1.99) — hasil sesuai spesifikasi.
- **Pelajaran deploy Vercel (tahap 5.3):** deployment pertama gagal dengan `Invalid supabaseUrl` walau env var sudah diisi, karena toggle **"Sensitive"** aktif pada `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`. Variable `NEXT_PUBLIC_*` harus **tidak** ditandai Sensitive (nilainya memang didesain publik/terlihat di browser) — kalau ditandai Sensitive, Next.js gagal meng-inline nilainya saat build. Solusi: hapus & buat ulang variable dengan toggle Sensitive OFF, lalu redeploy. **Catat ini di DEPLOYMENT.md untuk form-form berikutnya** supaya tidak terulang.

---

## MODUL MANAJEMEN PROYEK (PM) — akses terbatas ±3 orang

> Modul KEDUA, terpisah total dari modul form Seleksi Investasi/Quick Screen di atas: tabel relasional sendiri (prefix `pm_`), akses dibatasi lewat `pm_members`, UI di `components/pm/`. Spesifikasi fitur & skema lengkap ada di [PM-MODULE-SPEC.md](PM-MODULE-SPEC.md) — sumber kebenaran. Aturan main & alur kerja ada di [PROMPT-GABUNG-PM.md](PROMPT-GABUNG-PM.md). **JANGAN** mengubah tabel/RLS/kode modul form yang sudah ada di atas kecuali diminta eksplisit.

### FASE A — Inti

- [x] A.1 Skema database inti (`pm_workspaces`, `pm_workspace_members`, `pm_members`, `pm_spaces`, `pm_lists`, `pm_tasks`) + RLS — **dijalankan & diverifikasi** di project Supabase asli (3 migration lewat SQL Editor, admin PM pertama di-bootstrap manual ke `pm_members`); regresi nol dikonfirmasi: landing page & dashboard modul form (Seleksi Investasi + Quick Screen Proyek) tetap tampil normal tanpa error setelah migration
- [x] A.2 Nav & akses: menu modul PM di dashboard cuma muncul untuk user yang ada di `pm_members` — **diuji end-to-end** di browser sungguhan terhadap Supabase asli: `budi@gmail.com` (bukan anggota `pm_members`) tidak melihat link "Manajemen Proyek" & `/pm` menampilkan pesan akses ditolak; `fajar@wikagedung.id` (admin di `pm_members`) melihat link-nya dan berhasil masuk ke halaman placeholder `/pm` tanpa error
- [x] A.3 CRUD Workspace/Space/List (role admin/member, tanpa Guest) — **diuji end-to-end** di browser sungguhan terhadap Supabase asli sebagai admin PM (`fajar@wikagedung.id`): buat Workspace → tambah anggota Workspace → buat Space → buat List → ubah nama List → hapus List → hapus Space → keluarkan anggota → hapus Workspace, semua langkah redirect & refresh data benar tanpa error konsol. Keputusan desain: create/rename/delete Workspace serta kelola anggota Workspace **admin-only** (PM-MODULE-SPEC.md §1 "admin ... kelola Workspace"), CRUD Space/List/Task terbuka untuk semua anggota Workspace. Email anggota ditampilkan lewat fungsi RPC `pm_member_profiles`/`pm_workspace_member_profiles` (SECURITY DEFINER, masing-masing mengecek sendiri hak pemanggil) alih-alih minta admin mengetik nama manual. Regresi nol dikonfirmasi: dashboard & landing page modul form tetap normal (pesan "Role Anda belum diatur" untuk `fajar@wikagedung.id` di `/dashboard` adalah perilaku benar - dia user PM-only, tidak ada baris `user_roles` modul form, bukan bug)
- [x] Shell navigasi PM (di luar daftar asli PM-MODULE-SPEC.md §3, ditambah atas permintaan user 2026-07-10): `app/pm/layout.tsx` membungkus semua route `/pm/*` — sidebar persisten gaya ClickUp (pohon Workspace → Space → List, highlight path aktif) sebagai tampilan utama desktop, plus toggle manual "Tampilan Mobile"/"Tampilan Desktop" (bukan auto-responsive - preferensi tersimpan di cookie `pm_mobile_mode`, independen dari lebar layar) yang beralih ke layout sederhana bertumpuk (halaman A.1-A.3 apa adanya). **Diuji end-to-end**: sidebar ter-update otomatis setelah create/rename/delete Workspace/Space/List (perlu `revalidatePath("/pm", "layout")` di tiap Server Action terkait — tanpa itu sidebar tidak refetch data karena Next.js tidak otomatis re-render layout saat navigasi ke halaman di bawahnya), toggle mobile↔desktop berfungsi dua arah tanpa error konsol. Akses-ditolak (`!pmRole`) sekarang dicek sekali di layout, bukan diulang di tiap halaman.
- [x] A.4 CRUD Task (assignee, due date, status, priority) + Comment + Checklist — **diuji end-to-end** di browser sungguhan: buat Task (assignee dari anggota Workspace, due date, status, priority) → redirect ke Task Detail → tambah item checklist → tandai selesai (strikethrough benar) → tambah komentar (email penulis tampil) → hapus komentar → hapus item checklist → ubah status di form edit ("Simpan Perubahan") → dikonfirmasi berubah di tabel List → hapus Task, semua tanpa error konsol, regresi nol ke modul form. Tabel baru `pm_comments` (hapus hanya oleh penulis sendiri) & `pm_checklist_items` (akses penuh anggota Workspace, konsisten pola `pm_tasks`), keduanya pakai helper `pm_can_access_task()` yang sudah dibuat di A.1. List View di halaman List saat ini masih tabel sederhana (bukan tabel penuh dengan filter/sort) - itu cakupan A.5.
- [x] A.5 List View (tabel) + Board View (Kanban drag-and-drop) — **diuji end-to-end** di browser sungguhan: List View dapat filter (status/assignee/priority) + sort (judul/due date/terbaru dibuat) lewat query param, dikonfirmasi filter priority=urgent menyaring benar dari 3 Task jadi 1. Board View (`components/pm/PmBoardView.tsx`, Client Component, native HTML5 Drag & Drop API - tanpa dependency baru) render 4 kolom status; drag antar kolom mengubah status Task lewat Server Action `updateTaskStatus` (dipanggil langsung, bukan lewat `<form>`) + `router.refresh()`, dikonfirmasi 2x berpindah kolom dengan benar. **1 bug ditemukan & diperbaiki saat verifikasi**: fungsi (`taskHref`) sempat dilempar sebagai prop dari Server Component ke Client Component `PmBoardView` — Next.js melarang ini ("Functions cannot be passed directly to Client Components"), solusinya kirim string `listBase` dan bangun href di dalam Client Component. Reordering posisi Task dalam satu kolom (`urutan`) TIDAK diimplementasikan (di luar cakupan minimal "drag antar status").
- [x] A.6 Task Detail (modal/panel: comment, checklist) — klik Task dari List/Board sekarang membuka modal overlay (bukan navigasi halaman penuh), lewat pola Next.js App Router **Parallel Routes + Intercepting Routes** (`app/pm/[workspaceId]/[spaceId]/[listId]/layout.tsx` slot `@modal`, `@modal/default.tsx` mengembalikan null, `@modal/(.)[taskId]/page.tsx` intersep navigasi klien dan bungkus dengan `PmTaskModal` Client Component). Isi Task Detail diekstrak ke `components/pm/PmTaskDetailContent.tsx` (Server Component) supaya dipakai bersama oleh modal DAN halaman penuh `[taskId]/page.tsx` (dipertahankan untuk refresh/link langsung/share, sesuai perilaku standar pola ini) tanpa duplikasi logic. **Diuji end-to-end oleh user secara manual** (tool browser saya sempat down sementara karena gangguan environment, sudah pulih dan dites ulang, hasil konsisten): buka Task dari List/Board → modal muncul, List tetap di belakang; tutup lewat tombol ✕, klik backdrop, dan tombol Escape — ketiganya berfungsi; edit field/checklist/comment di dalam modal → modal tetap terbuka dengan data ter-update; buka URL Task langsung/refresh → halaman penuh muncul (bukan modal), sesuai desain.
- [x] A.7 Notifikasi in-app sederhana — cakupan sengaja minimal (belum Realtime/cron, itu B.3+): 2 event pemicu dibuat langsung di Server Action terkait (`lib/pm/notifications.ts`) - Task di-assign ke orang lain (create/update Task), dan komentar baru masuk ke Task yang assignee-nya bukan penulis komentar. Tabel `pm_notifications` baru (RLS: baca/tandai-dibaca/hapus cuma penerima sendiri; insert oleh siapapun yang punya akses ke Task terkait, karena notifikasi dibuat OLEH aktor UNTUK penerima lain). UI: badge "Notifikasi" (jumlah belum dibaca) di `app/pm/layout.tsx`, halaman `/pm/notifications` (tandai dibaca per-item & sekaligus semua). **Diuji end-to-end oleh user secara manual** (tool browser saya sempat down lagi saat tahap ini; user menambahkan `budi@gmail.com` sementara ke `pm_members` buat tes lintas-user, dites, lalu dihapus lagi): notifikasi muncul benar untuk penerima saat dapat assignment/komentar dari user lain (bukan diri sendiri), badge & tandai-dibaca berfungsi. Regresi nol dikonfirmasi user (form module tetap normal selama pengujian).

### FASE B — Lengkapi Fitur Kerja

- [x] B.1 Folder & Subtask — Skema: tabel `pm_folders` baru (Space → Folder → List, opsional — List tetap bisa langsung di bawah Space kalau `folder_id` null, perilaku lama tidak berubah); `pm_lists.folder_id` & `pm_tasks.parent_task_id` nullable (sudah disiapkan sejak A.1). RLS Folder pakai `pm_can_access_space()` yang sudah ada; RLS List/Task tidak berubah (tetap anchor ke `space_id`/`list_id`). Rute Folder pakai path literal `/pm/{ws}/{space}/folder/{folderId}` (bukan `[folderId]` sejajar `[listId]`, supaya tidak ambigu dengan segmen dinamis List). Sidebar (`app/pm/layout.tsx`) dirakit dari 4 query datar (bukan nested select PostgREST) karena `pm_lists` punya dua FK (space_id & folder_id) yang perlu dipisah jadi "List langsung di Space" vs "List di dalam Folder". Subtask: form tambah minimal (cuma judul, `createSubtask`), ditampilkan di `PmTaskDetailContent` sebagai section baru + indikator "Subtask dari {induk}" saat melihat Subtask itu sendiri. **Diuji end-to-end oleh user secara manual** (tool browser saya masih down): buat Folder → List di dalamnya tidak muncul di List langsung Space → sidebar tampilkan Folder (📁) bersarang dengan benar → rename/hapus Folder (cascade ke List di dalamnya) → tambah Subtask dari Task Detail → klik masuk ke Subtask → link "Subtask dari ..." ke induk benar. Semua berfungsi sesuai desain.
- [x] B.2 Calendar View & Gantt View + Task Dependency — Skema: `pm_tasks.start_date` (nullable, dibutuhkan Gantt karena due_date saja cuma titik, bukan rentang) & tabel `pm_task_dependencies` (kedua Task divalidasi harus di List yang sama di Server Action, bukan constraint DB; RLS cukup lewat `pm_can_access_task(task_id)`). List page dapat tab ke-3/4 ("Calendar"/"Gantt") di samping List/Board yang sudah ada, state lewat query param (`view`, `month` untuk navigasi bulan Calendar). `PmCalendarView` (grid bulanan, Task muncul di sel due_date) & `PmGanttView` (batang horizontal proporsional start_date→due_date, warna sesuai status) keduanya Server Component murni (tanpa client JS, konsisten pola halaman lain) — **keputusan cakupan**: Gantt sengaja TIDAK menggambar garis panah dependency (butuh routing SVG dinamis antar batang, kompleksitasnya tidak sepadan untuk "sederhana"); dependency ditampilkan sebagai daftar teks "Menunggu Task lain" / "Diperlukan oleh" di Task Detail. **Diuji end-to-end oleh user secara manual**: Task dengan Tanggal Mulai+Due Date tampil benar di Calendar (termasuk navigasi bulan) dan Gantt (batang proporsional); Dependency ditambah dari satu Task, muncul di kedua arah (Task lain & task ini), hapus dari salah satu sisi menghilangkan dari keduanya. Semua sesuai desain.
- [x] B.3 Real-time sync via Supabase Realtime (Postgres Changes) — Tabel `pm_tasks`/`pm_comments`/`pm_checklist_items` didaftarkan ke publication `supabase_realtime`. Komponen baru `components/pm/PmRealtimeRefresher.tsx` (Client Component, return null) subscribe `postgres_changes` filtered per List (`list_id=eq...`, dipasang di halaman List) dan per Task (`id=eq...`/`task_id=eq...` untuk pm_tasks/pm_comments/pm_checklist_items, dipasang di `PmTaskDetailContent` - otomatis ikut ke modal & halaman penuh Task Detail), lalu `router.refresh()` saat ada event apapun. **1 bug nyata ditemukan & diperbaiki saat verifikasi**: subscription awalnya berhasil ("SUBSCRIBED") tapi event tidak pernah sampai - penyebabnya realtime client belum diberi token akses user (`supabase.realtime.setAuth()`) sebelum `channel.subscribe()`, jadi RLS diam-diam menolak semua event (dievaluasi sebagai anon/auth.uid() null). Diperbaiki dengan `await supabase.auth.getSession()` lalu `setAuth()` sebelum bikin channel. **Diuji end-to-end dengan 2 tab browser sungguhan** (bukan cuma baca dokumentasi): ubah status Task di tab A langsung muncul di tab B (List view) tanpa reload; kirim komentar di tab A langsung muncul di tab B (Task Detail) tanpa reload. Regresi nol ke modul form.
- [x] B.4 Sistem @mention (user & task) — **keputusan cakupan**: aplikasi ini belum punya rich text editor, jadi mention ditulis manual sebagai teks (bukan autocomplete dropdown seperti ClickUp asli) - `@email@domain.com` untuk anggota Workspace, `#[Judul Task]` (persis, di List yang sama) untuk Task lain. Parsing & linkifikasi di `lib/pm/mentions.tsx` (regex, murni tampilan, tanpa validasi keamanan tambahan - RLS tetap yang menentukan akses sungguhan). Mention user memicu notifikasi baru (`pm_notification_type` tambah nilai `mention`), mention diri sendiri sengaja tidak memicu notifikasi (sama seperti task_assigned/task_commented). **Diuji end-to-end di browser sungguhan**: komentar berisi `@fajar@wikagedung.id` dan `#[Task Realtime Uji]` dikirim → mention user tampil sebagai highlight biru, mention Task jadi link yang benar ke Task tujuan, self-mention dikonfirmasi TIDAK membuat notifikasi baru, tanpa error konsol.
- [x] B.5 Docs (editor teks tertaut ke Task) — kolaborasi real-time (CRDT/Yjs via Broadcast) — dependency baru `yjs` (satu-satunya cara wajar melakukan CRDT sungguhan, tidak ada di codebase sebelumnya). **Keputusan cakupan**: editor teks polos (`<textarea>`) yang di-bind manual ke `Y.Text`, BUKAN rich text editor (ProseMirror/TipTap) - sesuai "editor teks" di spec, menghindari dependency besar yang tidak diminta. Diff antara ketikan lama/baru dihitung dengan pendekatan prefix/suffix sederhana (`applyTextDiff` di `PmCollaborativeDoc.tsx`) supaya operasi Yjs tetap granular per-karakter, bukan replace-semua/last-write-wins. Skema: tabel `pm_docs` baru, satu Doc per Task (dibuat lazy saat pertama dibuka, bukan trigger DB), `crdt_state bytea` untuk persistensi (encoding: hex `\x...` dari Postgres/PostgREST di baca, base64 dipakai sebagai format transport ke/dari Server Action - lihat `lib/pm/yjs-encoding.ts`). Sinkron LIVE pakai Supabase Realtime **Broadcast** (`channel.send`/`channel.on('broadcast', ...)`, BUKAN Postgres Changes seperti B.3) sesuai PM-MODULE-SPEC.md §5; state penuh disimpan ke DB berkala (debounce 1.5 detik) lewat Server Action `saveDocState`. Komponen `PmCollaborativeDoc` sengaja generik (props `docId`+`initialStateHex`, bukan `taskId`) supaya bisa dipakai ulang persis di Notulensi Meeting (C.4) sesuai spec. **Diuji end-to-end dengan 2 tab browser sungguhan**: ketik di tab A → muncul live di tab B tanpa reload (dan sebaliknya, terkonfirmasi 2 arah), lalu reload penuh mengonfirmasi teks tetap tersimpan di database (persistensi bukan cuma in-memory). Tanpa error konsol.
- [x] B.6 Custom fields & custom status per List — **keputusan cakupan penting untuk "custom status"**: List bisa mengganti LABEL TAMPILAN 4 status baku (to_do/in_progress/in_review/done) lewat `pm_lists.custom_status_labels` (jsonb, partial override), BUKAN mendefinisikan set status arbitrer baru per List seperti ClickUp asli. Kolom `pm_tasks.status` TETAP enum `pm_task_status` yang sama sejak A.4 - Board/Calendar/Gantt/filter yang sudah teruji tidak perlu dirombak, cuma teks yang ditampilkan (lewat `lib/pm/statusLabels.ts` → `mergeStatusLabels()`) yang berubah, di-thread konsisten ke semua tempat status ditampilkan (tabel List, filter, form buat/edit Task, kolom Board, badge Subtask) supaya tidak ada inkonsistensi label antar tampilan. Full custom status arbitrer akan berarti mengubah `status` dari enum ke referensi tabel - perubahan arsitektur besar yang sengaja ditunda. Custom Fields: tabel baru `pm_custom_field_definitions` (per List: nama, tipe teks/angka/tanggal/checkbox/pilihan, opsi buat tipe pilihan) & `pm_custom_field_values` (per Task), nilainya disimpan bareng dengan form "Detail Task" yang sudah ada (satu tombol Simpan, bukan form terpisah). **Diuji end-to-end di browser sungguhan**: buat Custom Field tipe Pilihan → muncul & bisa diisi di Task Detail → nilai tersimpan setelah reload; ganti label status ("To Do"→"Backlog", "Done"→"Selesai") → dikonfirmasi konsisten muncul di filter, tabel List, form buat Task, kolom Board, DAN status select Task Detail - tanpa satupun tempat yang masih menampilkan label lama. Tanpa error konsol, regresi nol ke modul form.
- [x] B.7 Dashboard: tab Progres Task, Workload, Resume — halaman baru `/pm/{workspaceId}/dashboard`, di-scope per Workspace (bukan gabungan lintas Workspace, selaras pola sidebar/breadcrumb yang sudah ada), diakses lewat link "Lihat Dashboard" di halaman Workspace. 3 tab lewat query param `?tab=`: **Progres Task** (jumlah & persentase Task per status, bar segmented), **Workload** (jumlah Task per anggota Workspace, termasuk yang belum ada assignee), **Resume** (4 tile: Selesai/Terlambat/Sesuai Jadwal/Tanpa Due Date - "terlambat" dihitung dari `due_date < hari ini AND status != done`, Task yang sudah selesai tidak pernah dihitung terlambat berapa pun tanggalnya). Tidak pakai Recharts (sudah ada di dependency form module) - progress bar & tile CSS polos sudah cukup untuk data hitungan sederhana ini, konsisten dengan estetika minimal aplikasi. Label status di dashboard ini pakai default (bukan `custom_status_labels` per List dari B.6) karena Task berasal dari banyak List sekaligus yang labelnya bisa berbeda-beda. **Diuji end-to-end di browser sungguhan** terhadap data nyata (2 Task: 1 Done tanpa due date, 1 To Do tanpa due date, keduanya tanpa assignee): ketiga tab menunjukkan angka yang cocok 100% dengan data sesungguhnya (Progres Task 50%/50%, Workload 0 Task+2 belum ada assignee, Resume 1 Selesai+1 Tanpa Due Date). Tanpa error konsol.
- [x] B.8 Search cepat (Command+K) — **keputusan cakupan**: cuma modul PM (spec menyebut "opsional, bisa dipisah dulu" untuk gabung dengan search modul form - modul form belum punya search sendiri sama sekali, jadi tidak ada yang perlu digabung untuk sekarang). Komponen `PmCommandPalette` (tombol "Cari ⌘K" + listener global Cmd/Ctrl+K, Escape untuk tutup) dipasang di `app/pm/layout.tsx` (tersedia di semua halaman PM, mode desktop maupun mobile). Pencarian (`actions/pm/search.ts`) pakai `ilike` lintas 4 tabel (Workspace/Space/List/Task) didebounce 250ms di klien - TIDAK pakai full-text search Postgres (`tsvector`) karena skala data kecil (~3 orang), `ilike` sederhana sudah cukup cepat & jauh lebih simpel. RLS yang sudah ada otomatis membatasi hasil ke yang boleh diakses, tidak perlu filter tambahan. Href tiap hasil dibangun dari rantai relasi (Task→List→Space→Workspace) lewat nested select PostgREST. **Diuji end-to-end di browser sungguhan**: buka palette lewat tombol, ketik "Task Realtime" → hasil "Task Realtime Uji" muncul dengan benar → klik hasil → navigasi ke Task yang tepat (dikonfirmasi lewat URL, bukan cuma judul) dengan data (termasuk nilai Custom Field dari B.6) tetap utuh. Tanpa error konsol.

### FASE C — Kolaborasi Tambahan

- [x] C.1 Automasi sederhana (Trigger-Condition-Action, per List) — Skema: tabel baru `pm_automations` (per List: nama, `trigger_status` enum `pm_task_status`, `condition_priority` enum `pm_task_priority` nullable/opsional, `action_type` text CHECK IN set_status/set_assignee/set_priority, `action_value` text, `aktif` boolean). **Keputusan cakupan**: bukan automation builder bebas seperti ClickUp asli - cuma 1 Trigger ("status Task berubah jadi X"), 1 Condition opsional ("priority = Y"), dan 3 jenis Action. Dijalankan SATU KALI dan TIDAK rekursif (`lib/pm/automations.ts` → `runAutomations()`, dipanggil dari `updateTask`/`updateTaskStatus` setelah status berhasil diubah, hasil action-nya sendiri tidak memicu automation lain lagi) - sengaja dibatasi begitu supaya tidak ada risiko infinite loop antar automation yang saling memicu. UI: section "Automasi" baru di halaman List (daftar automasi existing dengan tombol Aktifkan/Nonaktifkan & Hapus, form buat baru), pola sama seperti "Kelola Status"/"Custom Field" di atasnya. **Diuji end-to-end di browser sungguhan**: buat automasi "saat status → In Review, ubah status ke Done" → ubah status Task lain ke In Review lewat Detail Task → status otomatis loncat ke Done (dikonfirmasi lewat DOM & tabel List) → Nonaktifkan automasi → ulangi trigger yang sama → status kali ini TETAP di In Review (tidak auto-flip, toggle terbukti berfungsi) → Hapus automasi → daftar kembali kosong. Data uji dikembalikan ke kondisi semula setelah verifikasi. Tanpa error konsol, tsc & eslint bersih.
- [x] C.2 Template List (simpan & instansiasi ulang) — Skema: tabel baru `pm_list_templates` (per Workspace: nama, `custom_status_labels` jsonb, `custom_fields` jsonb). **Keputusan cakupan**: Template menyimpan SNAPSHOT (bukan referensi hidup) dari label status & definisi Custom Field List sumber saat tombol "Simpan sebagai Template" ditekan - kalau List sumbernya diedit/dihapus setelahnya, Template tetap utuh. List hasil instansiasi ulang selalu jadi anak langsung Space (bukan ke dalam Folder tertentu - bisa dipindah manual kalau perlu). RLS lewat `pm_can_access_workspace()`, member-level sama seperti CRUD Space/List. UI: halaman baru `/pm/{workspaceId}/templates` (daftar Template + form "Buat List dari Template" pilih Template & Space tujuan + nama List baru), link "Template List →" ditambahkan di halaman Workspace (sebelah "Lihat Dashboard"), section "Simpan sebagai Template" ditambahkan di halaman List sebelum "Pengaturan List". **Diuji end-to-end di browser sungguhan**: tambah Custom Field "Estimasi" + ganti label "To Do"→"Backlog" di List "Test" → Simpan sebagai Template → buka halaman Template, Template muncul dengan info "1 Custom Field" → Buat List baru dari Template ke Space lain (TIS) → List baru terbukti punya label "Backlog" dan Custom Field "Estimasi (Teks)" tanpa perlu diisi ulang manual → List hasil uji & Template dihapus, List "Test" asal dikembalikan ke kondisi semula (Custom Field & label direset). Tanpa error konsol, tsc & eslint bersih.
- [x] C.3 Whiteboard + konversi sticky note jadi Task — Skema: tabel `pm_whiteboards` (per Space) & `pm_whiteboard_items` (sticky note: konten, warna, `pos_x`/`pos_y` sebagai PERSENTASE 0-100 dari lebar/tinggi kanvas - bukan pixel absolut, supaya proporsional di ukuran layar berbeda - plus `task_id` nullable diisi saat dikonversi). Fungsi helper baru `pm_can_access_whiteboard()` (pola sama seperti `pm_can_access_list`). **Keputusan cakupan**: cuma sticky note (teks+warna+posisi bebas), BUKAN whiteboard penuh ClickUp asli yang juga punya shape/connector/gambar/media bebas. Real-time sync-nya pakai Supabase Realtime Postgres Changes (`pm_whiteboard_items` didaftarkan ke publication, sama seperti Task/Board B.3) - BUKAN Broadcast/CRDT seperti Docs (B.5), karena sticky note adalah objek diskrit mirip Task, bukan teks yang diketik karakter-demi-karakter bersamaan. Drag posisi sticky pakai native HTML5 Drag & Drop API (pola sama seperti Board View A.5, tanpa library eksternal) - posisi baru dihitung dari koordinat drop relatif terhadap kanvas lalu dikonversi ke persentase. Sticky yang sudah dikonversi TETAP ada di papan (`task_id` diisi, bukan dihapus) dan menampilkan link "→ Task: {judul}" ke Task hasil konversi, bukan form konversi lagi. UI: section "Whiteboard" baru di halaman Space (daftar + buat baru, pola sama seperti List/Folder), halaman kanvas baru `/pm/{workspaceId}/{spaceId}/whiteboard/{whiteboardId}` (`PmWhiteboardCanvas`, Client Component: klik area kosong = tambah sticky, textarea per-sticky dengan auto-save on blur, swatch 4 warna, tombol Hapus, form pilih List tujuan + "Jadikan Task"). **Diuji end-to-end di browser sungguhan**: buat Whiteboard → klik kanvas menambah sticky → isi teks tersimpan (dikonfirmasi setelah blur & refresh) → ganti warna ke pink tersimpan → drag sticky ke posisi lain (event drag native, diverifikasi dengan jeda antar tahap dragstart/dragover/drop supaya state React sempat ter-flush - PERSIS mekanisme yang akan terjadi pada drag asli pengguna) → posisi baru tersimpan & bertahan setelah refresh → "Jadikan Task" membuat Task sungguhan di List yang dipilih (dikonfirmasi judul & lokasi Task benar) → sticky berubah jadi link ke Task tsb. Whiteboard, Task, & sticky uji dihapus setelah verifikasi. Tanpa error konsol, tsc & eslint bersih.
- [x] C.4 Notulensi Meeting (editor real-time sama dengan Docs + action item → Task) — Skema: `pm_meetings` (per Workspace, `space_id` nullable buat pengelompokan opsional, `crdt_state bytea` LANGSUNG di baris ini - beda dengan Docs B.5 yang punya tabel `pm_docs` terpisah 1:1 per Task, karena Meeting cuma butuh satu Doc per baris, bukan banyak), `pm_meeting_attendees` (peserta, ditentukan SEKALI saat meeting dibuat - belum ada UI ubah peserta setelahnya), `pm_meeting_action_items` (deskripsi, assignee, due date, `task_id` nullable diisi saat dikonversi). Fungsi helper baru `pm_can_access_meeting()`. **Reuse eksplisit sesuai spec**: `PmCollaborativeDoc` (B.5) digeneralisasi dengan prop baru `kind: "doc" | "meeting"` (default "doc") yang menentukan Server Action persistensi mana yang dipanggil (`saveDocState` vs `saveMeetingState` baru) dan namespace channel Broadcast (`pm-doc-{id}` vs `pm-meeting-{id}`) - isi/logic CRDT-nya sendiri sama persis, tidak diduplikasi. Konversi Action Item → Task pakai pola identik `convertStickyToTask` (C.3): Task baru dibuat dengan assignee & due date ikut ter-isi dari Action Item, baris Action Item TETAP ada (`task_id` diisi, bukan dihapus) dan menampilkan link ke Task hasil konversi. UI: halaman riwayat `/pm/{workspaceId}/meetings` (daftar + form buat baru dengan judul/tanggal/Space opsional/checkbox pilih peserta dari anggota Workspace) dan halaman detail `/pm/{workspaceId}/meetings/{meetingId}` (badge peserta, editor notulensi kolaboratif, panel Action Item terpisah dari teks notulensi dengan form tambah + `PmActionItemList` Client Component buat Hapus/"Jadikan Task"), link "Notulensi Meeting →" ditambahkan di halaman Workspace. Meeting ikut masuk hasil Search cepat (B.8, `actions/pm/search.ts` & `PmCommandPalette` diperluas dengan tipe `"meeting"`) sesuai spec. **Diuji end-to-end di browser sungguhan**: buat Meeting dengan peserta → ketik notulensi, tersimpan & bertahan setelah reload (persistensi lewat Server Action, bukan cuma in-memory) → tambah Action Item dengan PIC & tenggat → "Jadikan Task" membuat Task sungguhan dengan assignee & due date yang identik (dikonfirmasi field-by-field) → Action Item berubah jadi link ke Task tsb → Meeting "Testt" muncul di hasil Command+K Search dengan badge tipe "Meeting" yang benar. Ditemukan & diperbaiki 1 masalah environment (bukan bug kode): cache build `.next` basi menyebabkan SEMUA rute literal (`/dashboard`, `/templates`, rute baru `/meetings`) 404 setelah dev server di-restart pertama kali sesi ini - diperbaiki dengan menghapus folder `.next` sebelum restart ulang, dikonfirmasi rute lama & baru kembali normal setelahnya. Task & Meeting uji dihapus setelah verifikasi. Tanpa error konsol, tsc & eslint bersih.
- [x] C.5 Time tracking manual (catat menit per Task) — Skema: tabel baru `pm_time_entries` (task_id, user_id, menit integer CHECK > 0, catatan opsional, tanggal date default hari ini). **Keputusan cakupan**: manual saja (user mengetik sendiri jumlah menit setelah kerja), BUKAN timer start/stop otomatis. `user_id` SELALU diri sendiri - dipaksa dari Server Action lewat `auth.getUser()` (bukan field yang bisa dipilih di form), dan RLS INSERT eksplisit mensyaratkan `user_id = auth.uid()` supaya tidak ada yang bisa mencatatkan waktu kerja atas nama orang lain lewat request langsung ke API pun. Hapus entri DIBATASI ke pembuatnya sendiri saja lewat RLS DELETE (pola sama seperti Komentar B.4, beda dengan Checklist yang bebas semua anggota) - baris "Hapus" di UI otomatis disembunyikan untuk entri milik orang lain. UI: section "Waktu Kerja" baru di Task Detail (`PmTaskDetailContent.tsx`, antara Checklist & Subtask) - daftar entri (durasi diformat "Xj Ym", email pencatat, tanggal, catatan opsional) + badge Total menit terakumulasi di judul section + form tambah (menit/tanggal/catatan). **Diuji end-to-end di browser sungguhan**: catat 90 menit → tampil sebagai "1j 30m" & Total "1j 30m" → catat 45 menit lagi → Total terhitung benar jadi "2j 15m" → hapus salah satu entri → Total & daftar ter-update benar → hapus entri terakhir → kembali ke "Belum ada waktu kerja tercatat.". Tanpa error konsol, tsc & eslint bersih.
- [x] C.6 File attachment via Supabase Storage — Bucket Storage baru `pm-attachments`, dibuat lewat migration SQL (`insert into storage.buckets`) - **PRIVATE** (bukan public), konsisten dengan semangat akses terbatas modul PM. Path penyimpanan pakai konvensi `{taskId}/{uuid}-{namaFile}` supaya RLS `storage.objects` bisa mengecek otorisasi lewat `pm_can_access_task()` dari segmen folder pertama (`storage.foldername(name)[1]`) tanpa tabel mapping tambahan. Tabel metadata `pm_attachments` terpisah (nama file asli, ukuran, tipe, pengunggah) supaya gampang di-query tanpa parsing storage tiap kali. **Keputusan cakupan**: unduh SELALU lewat signed URL berumur pendek (60 detik, dibangkitkan on-demand saat tombol "Unduh" diklik lewat Server Action yang me-redirect ke URL tsb) - BUKAN `getPublicUrl`, supaya file tidak pernah bisa diakses tanpa lewat otorisasi PM sama sekali. Validasi ukuran file maksimal 10 MB di sisi server (Server Action, bukan cuma client) sebelum upload ke Storage. Hapus lampiran menghapus DUA hal: baris `pm_attachments` DAN objek di Storage (`storage.remove()`) - kalau insert metadata gagal setelah upload berhasil, file yang sudah kadung terupload ikut dibersihkan (rollback manual) supaya tidak ada file yatim di bucket. UI: section "Lampiran" baru di Task Detail (daftar file dengan ukuran diformat KB/MB + email pengunggah + tombol Unduh/Hapus, form upload `<input type="file">` polos tanpa client JS). **Diuji end-to-end di browser sungguhan** (termasuk simulasi upload file sungguhan lewat DataTransfer API, bukan cuma cek UI): unggah file teks uji → muncul di daftar dengan ukuran & pengunggah benar → klik "Unduh" → browser benar-benar berpindah ke domain Supabase Storage dengan signed URL bertoken, isi file yang diunduh cocok 100% dengan isi yang diunggah (round-trip penuh, membuktikan RLS `storage.objects` mengizinkan akses yang sah) → Hapus → daftar kembali kosong. Tanpa error konsol, tsc & eslint bersih.
- [x] C.7 Ganti password, rename Workspace/Space/List — Rename Workspace/Space/List sudah ada sejak CRUD A.3, tidak ada kerja tambahan. **Temuan penting sebelum implementasi**: asumsi PM-MODULE-SPEC.md §3 C.7 ("kemungkinan besar pakai halaman akun yang sudah ada di aplikasi form ini") TERNYATA KELIRU - hasil eksplorasi menyeluruh mengonfirmasi modul form yang sudah ada SAMA SEKALI tidak punya halaman akun/profile/ganti-password (`actions/auth.ts` sebelumnya cuma berisi `login`/`logout`). Dikonfirmasi ke user lewat AskUserQuestion, dipilih: bangun halaman akun UMUM baru di `/akun` (bukan di-scope ke `/pm/akun`) supaya bisa dipakai SEMUA user aplikasi form ini, bukan cuma anggota PM - murni penambahan, tidak mengubah kode modul form yang sudah ada. Tidak ada migration/tabel baru (murni `supabase.auth.updateUser`, tidak ada skema PM yang tersentuh). **Desain keamanan**: `changePassword` (baru, `actions/auth.ts`) mewajibkan re-autentikasi lewat `signInWithPassword` pakai Password Saat Ini SEBELUM memanggil `updateUser({password})` - mencegah sesi yang lupa di-logout (browser publik/dipinjam) dipakai ganti password tanpa tahu password lama, bukan cuma mengandalkan sesi aktif. Validasi Zod: password baru minimal 8 karakter, konfirmasi harus cocok. Link "Akun" ditambahkan di 3 tempat: header beranda (`app/page.tsx`, di antara "Manajemen Proyek" dan "Keluar"), dan top bar `app/pm/layout.tsx` (mode desktop maupun mobile) supaya anggota PM juga gampang menemukannya. **Diuji end-to-end di browser sungguhan** (tanpa pernah menyentuh password asli akun produksi - keputusan sengaja demi keamanan): halaman `/akun` menampilkan email user dengan benar; submit dengan konfirmasi tidak cocok → pesan error "Konfirmasi password baru tidak cocok" (Zod, gagal sebelum reauth, password asli tidak tersentuh); submit dengan password baru < 8 karakter → pesan error minimal karakter; submit dengan Password Saat Ini yang SALAH tapi password baru valid → pesan "Password saat ini salah." (reauth gagal, `updateUser` tidak pernah terpanggil - dikonfirmasi password asli tetap utuh); link "Akun" muncul & mengarah benar di beranda maupun dalam modul PM. Jalur SUKSES ganti password (butuh password asli akun produksi) sengaja TIDAK diuji otomatis oleh saya - diserahkan ke user untuk dites manual sendiri. Tanpa error konsol, tsc & eslint bersih (1 warning pre-existing tak terkait di `app/forms/quick-screen-proyek` diabaikan, bukan bagian perubahan ini).

### Pemadatan UI (di luar checklist spec, permintaan user setelah Fase C selesai)

- [x] Pemadatan tampilan List/Task Detail/header PM supaya lebih ringkas (mirip ClickUp asli) — dipicu user membandingkan langsung dengan screenshot referensi ClickUp-clone (5 gambar: Calendar, Board, Task Detail modal, List, header). Dikonfirmasi cakupan lewat AskUserQuestion, user pilih ketiganya: (1) **Edit inline di tabel List** — Status/Priority/Due Date sekarang bisa diubah langsung dari dropdown/date-input di baris tabel tanpa buka Task Detail, lewat Client Component baru `PmTaskListInline.tsx` yang manggil Server Action langsung (pola sama seperti drag-drop Board A.5) - 2 action baru `updateTaskPriority`/`updateTaskDueDate` ditambahkan ke `actions/pm/tasks.ts` (mirror `updateTaskStatus` yang sudah ada, tidak memicu `runAutomations` karena trigger automasi cuma bereaksi ke status). (2) **Task Detail dipadatkan jadi SATU panel** — `PmTaskDetailContent.tsx` yang tadinya 8 kartu putih terpisah (masing-masing `rounded-3xl border shadow p-7/p-9` sendiri-sendiri, requiring banyak scroll) digabung jadi satu kartu dengan pembatas tipis antar-section (`border-t pt-5`), field Status/Priority/Start Date/Due Date jadi 1 baris grid 4 kolom (bukan tersebar vertikal), semua label/input/tombol diperkecil (text-[11px]-[13px], padding lebih tipis) - TIDAK ada perubahan pada action/handler apapun, murni penataan ulang JSX+className. (3) **Toolbar & header dipadatkan** — `app/pm/layout.tsx` top bar dipertipis (`py-2.5` + border-bottom, ditambah email user di kiri) untuk mode desktop maupun mobile; halaman List, 5 section besar yang tadinya kartu terpisah sepanjang halaman (Kelola Status, Custom Field, Automasi, Simpan sebagai Template, Pengaturan List) dipindah jadi popover kompak lewat elemen native `<details>/<summary>` di toolbar (⚙ Status, ⚡ Automasi, 💾 Simpan sbg Template, ⚙️ Pengaturan) - collapsed by default, TANPA Client Component/JS tambahan (browser native, konsisten dengan filosofi "server action + minimal JS" aplikasi ini); form "Buat Task baru" yang tadinya kartu besar 6 field disederhanakan jadi 1 baris input judul + tombol "Tambah" langsung di atas tabel (field lain diisi belakangan dari Task Detail setelah dibuat, matching alur ClickUp asli: quick-add lalu refine). **Diuji end-to-end di browser sungguhan**: edit inline Status di tabel List tersimpan & bertahan setelah refresh (diverifikasi lalu dikembalikan ke nilai semula); quick-add task baru berhasil membuat Task & redirect ke Task Detail; Task Detail terkonfirmasi sekarang satu panel menerus (Detail Task → Docs → Checklist → Waktu Kerja → Lampiran → Subtask → Dependency, semua nyambung tanpa card berulang); popover toolbar "Status" dan "Automasi" dikonfirmasi terbuka/tertutup dengan benar dan berisi form yang identik fungsinya dengan sebelumnya; Board view dikonfirmasi tetap normal dengan toolbar baru (regresi nol). Task uji dihapus setelah verifikasi. Tanpa error konsol, tsc & eslint bersih (1 warning pre-existing tak terkait di `app/forms/quick-screen-proyek` diabaikan).

### Pemadatan UI lanjutan (permintaan user: Workspace/Space/Folder/List + sidebar collapsible)

- [x] Padatkan halaman Workspace/Space/Folder + sidebar collapsible — Melanjutkan pola pemadatan yang sama dari List/Task Detail ke level di atasnya. Komponen baru `PmQuickAddForm.tsx` (Server Component murni, tanpa "use client" - cukup `<form>` + 1 input `nama` + tombol, action-nya Server Action yang dioper langsung sebagai prop antar Server Component, bukan lintas boundary Client) dipakai berulang untuk ganti kartu besar "Buat X baru" (Workspace/Space/Folder/List/Whiteboard) jadi satu baris input+tombol tepat di atas entity grid masing-masing. "Anggota Workspace" & "Pengaturan Workspace/Space/Folder" (rename+hapus) dipindah jadi popover toolbar `<details>` (pola sama seperti Status/Automasi di halaman List) - 👥 Anggota & ⚙️ Pengaturan di halaman Workspace, ⚙️ Pengaturan Space/Folder masing-masing di halaman Space/Folder. **Sidebar collapsible**: `PmSidebar.tsx` diberi tombol chevron (▾/▸) per Workspace/Space/Folder yang state expand-collapse-nya disimpan di `useState<Set<string>>` lokal komponen (bukan localStorage/DB - cukup untuk sesi karena `PmSidebar` tidak remount saat navigasi antar halaman di dalam layout `/pm`); node tanpa anak (Space/Folder kosong) tidak dapat chevron sama sekali (spacer kosong) supaya tidak ada tombol collapse yang tidak berguna. **Bug ditemukan & diperbaiki saat verifikasi**: popover "Anggota"/"Pengaturan" yang posisinya dekat sisi kanan toolbar (`absolute left-0`) terpotong keluar viewport - diperbaiki jadi `right-0` untuk popover yang dekat ujung kanan (Anggota/Pengaturan di halaman Workspace, Simpan sbg Template/Pengaturan di halaman List), popover yang dekat kiri (Status/Automasi) tetap `left-0`. **Diuji end-to-end di browser sungguhan**: navigasi Workspace→Space→Folder semua tampil ringkas (toolbar pill + quick-add + grid, tanpa kartu besar berulang); toggle collapse/expand di sidebar dikonfirmasi menyembunyikan/menampilkan anak node dengan benar; popover Anggota dikonfirmasi tidak lagi terpotong setelah perbaikan posisi. Tanpa error konsol, tsc & eslint bersih.

### Fitur tambahan: Task berulang (di luar checklist spec, permintaan user)

- [x] Task berulang - mingguan, bulanan, atau periode tertentu (hari) — Skema: 3 kolom baru di `pm_tasks` (`recurrence_type` enum `weekly`/`monthly`/`custom` nullable = tidak berulang, `recurrence_interval` integer "setiap N minggu/bulan/hari", `recurrence_end_date` opsional buat kapan berhenti). **Keputusan desain**: recurrence me-reset TASK YANG SAMA (bukan bikin Task baru tiap siklus) - saat Task berulang ditandai "Done", `applyRecurrenceIfDone()` (baru, `actions/pm/tasks.ts`) otomatis mengembalikan status ke "To Do" dan menggeser `due_date` maju sesuai aturan (`start_date` ikut digeser proporsional kalau terisi), dipanggil dari KEDUA jalur ubah status yang sudah ada (`updateTask` form lengkap & `updateTaskStatus` direct-call dari tabel List/Board) supaya konsisten dari jalur manapun status berubah jadi Done. Kalau siklus berikutnya sudah lewat `recurrence_end_date`, Task dibiarkan tetap "Done" (berhenti berulang secara alami). Helper murni penghitung tanggal ada di `lib/pm/recurrence.ts` (dipisah dari action, gampang diuji terpisah). UI: field "Ulangi" baru di Task Detail (setelah grid Status/Priority/Start/Due Date) + indikator 🔁 di judul Task pada tabel List dan kartu Board untuk Task yang sedang berulang. **Bug ditemukan & diperbaiki saat verifikasi pertama**: implementasi awal `computeNextDueDate` memakai `new Date(dateStr + "T00:00:00")` (diinterpretasi waktu LOKAL) lalu `.toISOString()` (konversi ke UTC) - di server dengan timezone WIB (UTC+7) ini menggeser tanggal MUNDUR satu hari (13 Jul + 7 hari harusnya 20 Jul, malah tersimpan 19 Jul). Diperbaiki dengan aritmetika tanggal murni UTC (`Date.UTC()` + `setUTCDate`/`setUTCMonth`), tidak lagi bergantung timezone proses Node manapun. **Diuji end-to-end di browser sungguhan** (setelah perbaikan bug): set Task berulang Mingguan dengan Due Date 13 Jul → tandai Done via form Task Detail → due date benar maju ke 20 Jul, status kembali To Do → diulang via dropdown Status inline di tabel List (jalur kode berbeda) → maju benar ke 27 Jul → diuji batas `recurrence_end_date` (diset sebelum siklus berikutnya) → Task tetap "Done", tidak lanjut berulang (dikonfirmasi berhenti wajar). Data uji dikembalikan ke kondisi semula. Tanpa error konsol, tsc & eslint bersih.

### FASE D — Penutup

- [x] D.1 Testing menyeluruh — Setiap tahap (A.1 s/d fitur Task berulang) sudah diuji end-to-end di browser sungguhan langsung setelah diimplementasi (bukan ditunda ke akhir), jadi D.1 di sini fokus pada verifikasi build production & regresi lintas-modul sebelum deploy. Dijalankan `npm run build` penuh (bukan cuma `next dev`) - berhasil compile bersih untuk SEMUA rute termasuk seluruh modul PM (`/pm`, `/pm/[workspaceId]/...` sampai ke Whiteboard/Meeting/Template/Dashboard) maupun modul form yang sudah ada (`/forms/seleksi-investasi/...`, `/forms/quick-screen-proyek/...`, `/dashboard`, `/akun`) - tidak ada error TypeScript/build di satupun rute, mengonfirmasi modul PM tidak merusak modul form (kode terpisah total, tidak ada import silang). tsc & eslint bersih di seluruh repo (1 warning pre-existing tak terkait di `app/forms/quick-screen-proyek` sejak sebelum modul PM ada, bukan regresi).
- [x] D.2 Deploy — push ke repo & Vercel project yang sama, tidak ada setup baru. 34 commit di-push ke `origin/master` (`81ed0aa..f338074`), Vercel auto-deploy lewat integrasi GitHub yang sudah ada (tidak ada perubahan config/env var). **Diverifikasi setelah deploy**: aset build baru ter-load 200 OK di `form-seleksi-investasi.vercel.app`, halaman `/login` normal, `/pm` diarahkan ke `/login` dengan benar untuk user belum login (bukan error 500 - mengonfirmasi rute PM & middleware auth ikut ter-deploy tanpa crash), tanpa error konsol. **User login langsung di production dan mengonfirmasi normal** (link "Manajemen Proyek" muncul, modul PM terbuka seperti di lokal).

**Modul Manajemen Proyek (PM) LIVE di production**: [form-seleksi-investasi.vercel.app](https://form-seleksi-investasi.vercel.app), Fase A-D selesai + fitur tambahan (UI compaction, Task berulang) di luar checklist awal.

### Optimasi performa (permintaan user: "terlalu lambat, refresh")

- [x] Kurangi jumlah & urutan query database per navigasi/refresh — **Penyebab utama**: `app/pm/layout.tsx` (sidebar) jalan ULANG di SETIAP `router.refresh()` di seluruh modul PM (dipanggil hampir di semua interaksi: centang checklist, drag Board, edit inline tabel List, dsb) - sebelumnya butuh sampai 4 query BERURUTAN (`getPmMembership()` sendiri manggil `auth.getUser()`, lalu layout manggil `auth.getUser()` LAGI secara terpisah = 1 round-trip auth yang duplikat; lalu workspaces → spaces → folders+lists, masing-masing menunggu hasil sebelumnya). Diperbaiki: `auth.getUser()` cuma dipanggil SEKALI (cek `pm_members` di-inline langsung di layout, bukan lewat `getPmMembership()` terpisah), `pm_members` & hitung notifikasi belum dibaca dijalankan PARALEL (`Promise.all`), dan query Workspace+Space digabung jadi SATU lewat nested select PostgREST (`pm_workspaces.select("...,pm_spaces(...)")` - tidak ambigu karena `pm_spaces` cuma 1 FK ke `pm_workspaces`) - totalnya dari ~4 round-trip berurutan jadi ~2. Folder+List tetap 2 query terpisah (bukan nested) karena `pm_lists` punya DUA FK (`space_id` & `folder_id`) yang butuh filter "langsung di Space vs di dalam Folder" yang lebih jelas dirakit manual daripada lewat select embedded. **Task Detail** (halaman terberat): query `pm_docs` (cek Doc kolaboratif sudah ada atau belum) sebelumnya jalan SETELAH batch query itu selesai (nambah 1 round-trip tambahan tiap render, bukan cuma sekali) - digabung masuk ke batch `Promise.all` yang sama karena tidak bergantung hasil query lain, cuma `taskId`. **Tidak ada perubahan skema/fitur** - murni pengurangan jumlah & pengaturan ulang urutan query, seharusnya tidak ada risiko `unstable_cache`/config global yang lebih berisiko (dipertimbangkan tapi TIDAK dipakai - butuh `cacheComponents: true` di `next.config.ts` yang akan mengubah perilaku render SELURUH aplikasi termasuk modul form, dan berisiko kebocoran data lintas-user kalau kunci cache tidak hati-hati mengikutsertakan identitas user; dianggap terlalu berisiko untuk perbaikan cepat ini). **Diuji di browser sungguhan terhadap data production nyata** (tim sudah pakai aplikasi ini aktif - Workspace "PORTOFOLIO BUSINESS" dengan Space/Folder/List/Task sungguhan): struktur sidebar bersarang (Workspace→Space→Folder→List) tampil benar setelah query digabung, halaman Task Detail dengan Docs tetap berfungsi normal setelah query paralel - **tanpa mengubah data tim** (murni baca, tidak ada mutasi saat verifikasi). Tanpa error konsol, tsc & eslint bersih.
- [x] Ronde ke-2: konsolidasi query Task Detail lebih lanjut — "Subtask" (`parent_task_id = taskId`) dan "Task lain di List ini" (kandidat Dependency + lookup `#[Judul Task]` mention) sebelumnya 2 query terpisah ke `pm_tasks` padahal sumbernya sama (`list_id` yang sama) - digabung jadi SATU query (ambil semua Task di List itu sekali), Subtask & "Task lain" diturunkan/dipisah di JS. "Menunggu Task lain" & "Diperlukan oleh" sebelumnya 2 query terpisah ke `pm_task_dependencies` (`task_id = X` vs `depends_on_task_id = X`) - digabung jadi satu lewat `.or("task_id.eq.X,depends_on_task_id.eq.X")`, dipisah di JS sesuai sisi mana yang match. Total query Task Detail turun dari ~14 jadi ~12. Diuji di browser sungguhan (Task Detail data production, termasuk Dependency & Subtask) - tetap tampil benar, tanpa error konsol, tsc & eslint bersih.

### Fitur tambahan: Multi-assignee & mention autocomplete (permintaan user)

- [x] Task multi-assignee — satu Task bisa ditugaskan ke lebih dari satu orang (sebelumnya `pm_tasks.assignee_id` cuma 1 orang). Skema: tabel baru `pm_task_assignees` (task_id + user_id), data `assignee_id` yang sudah ada di-backfill otomatis ke tabel baru sebelum kolom lama DIHAPUS (bukan dipertahankan berdampingan - supaya tidak ada 2 sumber kebenaran yang bisa tidak sinkron). Semua titik yang membaca/menulis assignee disesuaikan: form Task Detail (checkbox multi-pilih, bukan dropdown), tabel List & kartu Board (tampil "email1, email2"), filter assignee di List (query 2 langkah: cari task_id yang match dulu, baru `.in()` ke query utama - LIHAT catatan bug di bawah), notifikasi assign (`notifyTaskAssigned`/`notifyTaskCommented` di `lib/pm/notifications.ts` diubah terima array, bukan 1 id - assignee lama tidak dapat notifikasi ulang tiap Task diedit, cuma yang BARU ditambahkan), automasi "set_assignee" (tetap semantik REPLACE - ganti seluruh assignee dengan 1 orang target, kelanjutan wajar dari perilaku lama), konversi Meeting Action Item → Task, dan Dashboard Workload (hitung Task per anggota lewat array `assignee_ids`, 1 Task bisa masuk hitungan beberapa orang sekaligus). **2 bug ditemukan & diperbaiki saat verifikasi**: (1) tepat setelah migration dijalankan, query nested-select `pm_tasks.select("...,pm_task_assignees(user_id)")` gagal dengan error PostgREST `PGRST200` ("Could not find a relationship... in the schema cache") - penyebabnya cache skema PostgREST di Supabase belum di-refresh otomatis setelah tabel baru dibuat lewat SQL Editor manual (beda dengan migration tool resmi yang otomatis kirim notifikasi reload); diperbaiki user menjalankan `NOTIFY pgrst, 'reload schema';` di SQL Editor. (2) Filter assignee di halaman List awalnya pakai `!inner` + `.eq("pm_task_assignees.user_id", X)` di query yang SAMA - ternyata ini juga MEMFILTER ISI array assignee yang ditampilkan (kolom Assignee di tabel jadi cuma nampilin 1 assignee yang match filter, bukan semua assignee Task itu) - diperbaiki jadi query 2 langkah (cari task_id yang match dulu secara terpisah, baru `.in("id", taskIds)` di query utama TANPA `!inner`) supaya kolom Assignee tetap menampilkan assignee lengkap walau sedang difilter. **Diuji end-to-end di browser sungguhan** memakai Task uji baru (bukan data tim asli - user diminta pilih & memilih opsi "buat Task uji baru, hapus setelah selesai" lewat AskUserQuestion, sistem otorisasi sempat memblokir percobaan pertama yang tanpa sadar akan menyunting Task produksi asli "Konfirmasi surat magang"): centang 2 assignee sekaligus di Task Detail → tersimpan & bertahan setelah reload → tabel List & kartu Board menampilkan "email1, email2" → filter per-assignee tetap menampilkan assignee LENGKAP (bukan cuma yang difilter) → Dashboard Workload tetap akurat. Task uji dihapus setelah verifikasi, Task tim asli tidak tersentuh. Tanpa error konsol, tsc & eslint bersih.
- [x] Autocomplete "@mention" di Komentar — sebelumnya @mention cuma teks bebas yang diparsing SETELAH submit (`lib/pm/mentions.tsx`, regex), tanpa bantuan ketik. Komponen Client baru `PmMentionTextarea.tsx`: saat user ketik "@" diikuti huruf, muncul dropdown saran email anggota Workspace yang cocok (maks 5, filter substring case-insensitive), bisa navigasi dengan panah atas/bawah + Enter/Tab untuk pilih, Escape untuk tutup. Pilih saran cuma menyisipkan teks `@email ` ke posisi kursor (replace bagian "@xyz" yang sedang diketik) - **parsing/penyimpanan/notifikasi mention TIDAK berubah sama sekali**, murni bantuan UI saat mengetik, mekanisme lama di `createComment` tetap yang menentukan siapa benar-benar dapat notifikasi. Cakupan sengaja cuma trigger "@" (mention user) sesuai yang diminta - trigger "#" (mention Task) belum dapat autocomplete serupa, bisa ditambah belakangan kalau dibutuhkan. **Diuji end-to-end di browser sungguhan**: ketik "@w" → dropdown tampil (benar menampilkan ke-3 anggota karena ketiga email tim memang sama-sama mengandung huruf "w") → ketik lanjut "@win" → dropdown otomatis mengerucut ke 1 hasil ("windi.s@wgmail.id") → klik pilih → teks tersisip benar dengan spasi setelahnya → komentar dikirim & tersimpan dengan teks mention di dalamnya. Tanpa error konsol, tsc & eslint bersih.

### Umpan balik interaksi ringan (permintaan user: tombol/bagian yang dipilih tidak terdeteksi)

- [x] Animasi ringan saat tombol/elemen ditekan + state "aktif" jelas untuk popover toolbar — **Penyebab**: kebanyakan tombol modul PM sudah punya `transition-colors` (hover), tapi TIDAK ADA reaksi APAPUN pas benar-benar ditekan (state `:active`), dan tombol toolbar popover (⚙ Status/⚡ Automasi/💾 Template/⚙️ Pengaturan di halaman List/Workspace/Space/Folder) tetap terlihat SAMA PERSIS baik saat popover-nya terbuka maupun tertutup - tidak ada cara tahu "bagian mana yang lagi dipilih" cuma dari lihat tombolnya. Diperbaiki 2 hal: (1) stylesheet baru `app/pm/pm.css` (di-scope lewat class `.pm-app` yang dipasang di root `app/pm/layout.tsx`, BUKAN global - supaya modul form yang sudah ada sama sekali tidak tersentuh) nambah transisi + efek "tertekan" (`scale(0.96)` saat `:active`) ke semua `button`/`summary`/`a[href]`/checkbox di seluruh modul PM sekaligus, tanpa perlu edit satu-satu ke puluhan file. (2) Semua `<summary>` popover toolbar (`<details className="group">`) ditambah varian Tailwind `group-open:bg-zinc-900 group-open:text-white` supaya background-nya jadi GELAP PADAT begitu popover-nya terbuka, kembali abu-abu terang begitu ditutup - beda jauh & jelas kelihatan, bukan cuma transisi warna hover yang sama untuk kedua state. **Diuji di browser sungguhan**: klik "⚙ Status" di halaman List → tombolnya berubah jadi hitam pekat + teks putih selama popover terbuka, kembali abu-abu terang setelah ditutup - dikonfirmasi lewat screenshot before/after. Halaman List & Task Detail dicek ulang, semua checkbox/tombol/input tetap tampil normal (tidak ada regresi visual dari CSS baru). Tanpa error konsol, tsc & eslint bersih.
- [x] Perluas animasi ke kartu Task/Folder/List/Whiteboard (susulan permintaan user) — Ronde pertama cuma menyentuh tombol/popover; kartu ENTITAS (grid Workspace/Space/Folder/List/Whiteboard di `PmEntityGrid.tsx`, dan kartu Task di `PmBoardView.tsx`) belum kebagian efek tertekan sama sekali. **Keputusan teknis**: kartu-kartu ini SUDAH punya efek hover-angkat sendiri lewat Tailwind (`hover:-translate-y-0.5`) - nambah `:active` scale generik dari `pm.css` berisiko baku-hantam CSS `transform` antara hover-translate & active-scale (dua rule beda sumber berebut properti yang sama). Diselesaikan dengan class penanda baru `.pm-card` + rule khusus di `pm.css` (`div.pm-card:active { transform: translateY(0) scale(0.98); box-shadow: ...ringan... }`) yang SENGAJA override balik posisi hover-lift + mengecilkan sedikit saat ditekan - dijamin menang lewat specificity (2 class + `:active` > 1 class hover Tailwind), bukan gambling urutan file CSS. Kartu Task Board sekaligus dapat tambahan `hover:-translate-y-0.5 hover:shadow-...` yang sebelumnya belum ada sama sekali (cuma static shadow, tanpa hover feedback apapun). Link polos tanpa efek hover sendiri (baris Task di tabel List, item sidebar) tetap dapat `:active` scale generik seperti sebelumnya (dikecualikan dari `.pm-card` lewat selector `a[href]:not(.pm-card)`). **Diuji di browser sungguhan**: kartu Folder "UNIVERSITAS" & Whiteboard "Brain Storm" di halaman Space tampil normal; kartu Task "Konfirmasi surat magang" di Board view tampil normal dengan hover/shadow baru; tanpa regresi layout. Tanpa error konsol, tsc & eslint bersih.

### Bug fix: "Task tidak ditemukan" saat navigasi kembali (laporan user + screenshot mobile)

- [x] Modal Task Detail tidak punya jalan keluar jelas saat menampilkan state error — User melaporkan (lewat screenshot mobile) muncul notifikasi "Task tidak ditemukan (atau Anda tidak berwenang melihatnya)" saat memilih menu untuk kembali ke menu sebelumnya, dengan tampilan modal mengambang di atas halaman Task Detail lain yang buram di belakangnya - mengindikasikan slot modal (`@modal`, pola Parallel + Intercepting Routes untuk Task Detail) sempat menampilkan Task/state yang salah tanpa cara jelas untuk kembali (sebelum perbaikan, `PmTaskDetailContent.tsx` cuma menampilkan teks polos tanpa link apapun saat guard `!task || task.list_id !== listId` kena). **Investigasi**: sempat dicoba ganti mekanisme tutup modal di `PmTaskModal.tsx` dari `router.back()` (bawaan) ke `router.push(closeHref)` supaya deterministik (selalu menuju URL List yang pasti valid, tidak bergantung riwayat browser) - tapi diverifikasi lewat inspeksi DOM langsung (`window.location.href` vs elemen modal yang masih ter-render) bahwa `push()` ke URL yang TIDAK match rute eksplisit apapun di slot `@modal` TIDAK membersihkan slot tsb saat soft navigation (`default.tsx` cuma berlaku untuk hard nav/refresh, dikonfirmasi lewat `node_modules/next/dist/docs/.../parallel-routes.md` sesuai arahan AGENTS.md) - modal lama tetap nempel walau URL bar sudah berpindah, PERSIS gejala yang dilaporkan user. Dicoba perbaiki lebih lanjut dengan menambah rute catch-all (`@modal/page.tsx` + `@modal/[...catchAll]/page.tsx`, keduanya return `null`) sesuai pola resmi di dokumentasi Next.js untuk kasus modal+Link/push - tapi ini malah membuat Turbopack dev server error saat mencocokkan intercepting route (`Invalid interception route: .../(.)(.)(.)(.)(.)(.)(.)(.)...`, request jadi flaky 500/200 bergantian) karena konflik dengan rute intercepted `(.)[taskId]` yang sudah ada di slot yang sama. **Keputusan akhir**: batalkan perubahan mekanisme tutup modal (`PmTaskModal.tsx` kembali ke `router.back()` sesuai dokumentasi resmi, cuma ditambah komentar penjelasan kenapa BUKAN `push()` - functionally tidak berubah dari semula), rute catch-all dihapus total. Perbaikan sebenarnya difokuskan ke `PmTaskDetailContent.tsx`: state "Task tidak ditemukan" sekarang selalu menyertakan link "← Kembali ke List" yang mengarah ke URL List yang valid (`/pm/{workspaceId}/{spaceId}/{listId}`) - berlaku baik saat dirender di dalam modal maupun halaman penuh, jadi apapun penyebab tepatnya Task/slot yang salah muncul, user selalu punya jalan keluar yang jelas alih-alih dead-end. **Diuji di browser sungguhan** (server dev di-restart bersih tanpa cache basi tiap kali sebelum verifikasi, karena Fast Refresh/HMR sempat menyebabkan hasil klik yang tidak konsisten selama investigasi): buka modal Task "Konfirmasi surat magang" dari List → tutup via tombol X → kembali bersih ke List (dikonfirmasi lewat screenshot, tanpa modal nyangkut). Navigasi langsung ke URL Task dengan `taskId` palsu (`00000000-...`) → tampil "Task tidak ditemukan" + link "← Kembali ke List" → diklik → mendarat bersih di halaman List yang benar. Tanpa error konsol, tsc & eslint bersih.

### Optimasi responsivitas menyeluruh, ronde ke-3 (permintaan user: "optimalkan keseluruhan aplikasi... tanpa mengurangi/menghilangkan fitur")

- [x] Konsolidasi query di halaman-halaman yang belum tersentuh ronde optimasi sebelumnya (Dashboard, Meeting, Whiteboard, Template, Folder) — Disurvei lewat agen Explore read-only dulu untuk memetakan query sekuensial independen (kandidat `Promise.all`) & waterfall N+1 (kandidat nested/embedded select) tanpa menyentuh kode. **Dashboard Workspace** (`app/pm/[workspaceId]/dashboard/page.tsx`, ditemukan paling berat - 5 query berurutan): waterfall `pm_spaces → pm_lists → pm_tasks` 3 tahap diringkas jadi SATU nested select (`pm_spaces.select("id,pm_lists(id,pm_tasks(id,status,due_date,pm_task_assignees(user_id)))")`, ditelusuri lewat `.flatMap()` di JS), digabung paralel dengan fetch `pm_workspaces` & rpc `pm_workspace_member_profiles` yang sebelumnya juga berurutan padahal independen - total round-trip dari 5 jadi 2. **Meeting Detail** (`meetings/[meetingId]/page.tsx`): Task terkait tiap Action Item (`pm_meeting_action_items.task_id`) sebelumnya query terpisah SETELAH actionItems selesai (perlu tahu `task_id`-nya dulu) - diganti jadi nested select langsung di query Action Item (`...,pm_tasks(id,judul,list_id)`), begitu juga Space+List yang sebelumnya 2 query berurutan digabung nested (`pm_spaces.select("id,nama,pm_lists(id,nama,space_id)")`) - total dari 3 round-trip berurutan (setelah batch awal) jadi 0, semua masuk 1 `Promise.all` di awal. **Whiteboard** (`whiteboard/[whiteboardId]/page.tsx`): pola sama seperti Meeting - Task terkait tiap sticky note (`pm_whiteboard_items.task_id`) di-embed langsung (`...,pm_tasks(id,judul,list_id)`), menghilangkan 1 round-trip dependen. **Folder** (`folder/[folderId]/page.tsx`): fetch Folder + List-nya (2 query berurutan) digabung nested select (`pm_folders.select("...,pm_lists(id,nama,deskripsi)")`, tidak ambigu karena `pm_lists.folder_id` cuma merujuk 1 Folder). **Meetings list & Templates** (`meetings/page.tsx`, `templates/page.tsx`): fetch `pm_workspaces` (buat cek Workspace ada/tidak) yang sebelumnya berjalan sendiri sebelum batch, dipindah masuk ke `Promise.all` yang sama, dicek `null` setelahnya. **Tidak ada perubahan skema/fitur** - murni pengurangan & penggabungan query, hasil data yang ditampilkan identik dengan sebelumnya. **Diuji di browser sungguhan terhadap data production** (Workspace "PORTOFOLIO BUSINESS"): Dashboard - ketiga tab (Progres Task/Workload/Resume) menampilkan angka yang benar (1 Task ke `alexander.b@wgmail.id`, 1 Task tanpa Due Date, cocok dengan data Task uji yang masih ada); Meeting "Meeting Koordinasi" & Whiteboard "Brain Storm" tampil normal; Folder "UNIVERSITAS" menampilkan ketiga List (UI/UGM/UNAND) dengan urutan benar; Template List tampil normal. Tanpa error server/konsol di semua halaman yang diuji, tsc & eslint bersih.
- [x] `loading.tsx` baru di `app/pm/loading.tsx` — sebelumnya TIDAK ADA satupun `loading.tsx` di seluruh modul PM (dikonfirmasi lewat survei di atas), jadi setiap perpindahan halaman (List/Task/Dashboard/Meeting/dst) layar kosong penuh sampai data server selesai diambil, tanpa skeleton/indikasi apapun sedang memuat - murni kosmetik untuk kecepatan yang DIRASAKAN, tidak mengubah data atau logika fetch apapun. Ditempatkan di level `app/pm/` (bukan per-segment) supaya jadi fallback bersama untuk seluruh rute di bawahnya yang belum punya `loading.tsx` lebih spesifik sendiri. tsc & eslint bersih.

### Fitur baru: Dashboard Workspace - tampilan List/Board/Calendar agregat lintas List (permintaan user)

- [x] 3 tab baru di Dashboard Workspace (`app/pm/[workspaceId]/dashboard/page.tsx`) - "List", "Board", "Calendar" - menampilkan SELURUH Task di Workspace (lintas Space & List sekaligus), bukan cuma angka statistik agregat seperti 3 tab lama (Progres Task/Workload/Resume). **Reuse, bukan duplikasi**: dipakai ulang `PmBoardView`/`PmCalendarView`/`PmTaskListInline` yang sama persis dengan yang dipakai halaman List (drag-and-drop status di Board, edit inline Status/Priority/Due Date langsung di baris tabel List, semuanya tetap jalan apa adanya karena action-nya `updateTaskStatus`/`updateTaskPriority`/`updateTaskDueDate` cuma butuh `taskId`, tidak bergantung List/Space tertentu). **Masalah desain utama**: ketiga komponen itu awalnya cuma terima 1 `listBase` string buat semua Task (karena di halaman List semua Task memang dari 1 List yang sama) - di Dashboard, Task datang dari BANYAK List/Space berbeda jadi tiap Task butuh URL sendiri-sendiri. Percobaan pertama pakai prop function `taskHref: (task) => string` GAGAL di runtime dengan error Next.js "Functions cannot be passed directly to Client Components" - `PmBoardView` & `PmTaskListInline` keduanya `"use client"`, dan function tidak bisa lolos RSC boundary dari Server Component pemanggilnya. **Diperbaiki**: field `href?: string` disematkan LANGSUNG di tiap objek Task (data, bukan function) - `PmDashboardTask.href` dihitung sekali di server (`/pm/{workspaceId}/{space.id}/{list.id}/{task.id}`) saat query nested select di-flatten, lalu ketiga komponen tinggal pakai `task.href ?? \`${listBase}/${task.id}\`` (fallback ke perilaku lama kalau `href` tidak diisi - dipastikan tidak regresi ke halaman List yang sudah ada). Query Task Dashboard yang sudah ada dari ronde optimasi sebelumnya diperluas (tambah `judul`, `priority`, `recurrence_type` yang sebelumnya tidak diambil karena cuma dipakai buat statistik agregat). Label status tetap pakai `TASK_STATUS_LABEL` default (bukan custom per-List) - keputusan yang sama seperti 3 tab lama, karena Task Dashboard berasal dari List-List dengan kemungkinan label kustom berbeda-beda. **Sengaja TIDAK ada form "Tambah Task"** di tab List/Board Dashboard (beda dari halaman List) - Task baru butuh List tujuan yang jelas, ambigu kalau dibuat dari tampilan agregat lintas-List; Dashboard di sini murni utk monitoring bukan entry point pembuatan Task baru. Tab "Calendar" butuh param nama query berbeda dari halaman List (List pakai `?view=`, Dashboard pakai `?tab=` utk 3 tab lama) - `PmCalendarView` ditambah prop opsional `viewParamKey` (default `"view"`) supaya link navigasi bulan tetap konsisten dgn skema masing-masing halaman pemanggil. **Diuji end-to-end di browser sungguhan** (Workspace "PORTOFOLIO BUSINESS", data production nyata, murni baca - tidak ada mutasi): tab List menampilkan Task dari List "UI", "UNAND" (Space "MITRA STRATEGIS"), dan List "Mitra Strategis" (Space "BUSINESS CLINIC") sekaligus dalam satu tabel; diklik salah satu Task dari Space/List yang berbeda-beda → mendarat tepat di halaman Task Detail yang benar (path space/list/task sesuai asalnya). Tab Board menampilkan kolom To Do/In Progress dengan kartu Task lintas List. Tab Calendar menampilkan bulan berjalan dengan navigasi bulan pakai `?tab=calendar&month=...` (bukan `?view=`, dikonfirmasi). Regresi-check: halaman List asli (`?view=board`, `?view=calendar`) dicoba ulang, tetap tampil & berfungsi normal (link Task pakai fallback `listBase` seperti semula, tidak kebawa `href`/`viewParamKey` dari perubahan Dashboard). Tanpa error server/konsol, tsc & eslint bersih.
